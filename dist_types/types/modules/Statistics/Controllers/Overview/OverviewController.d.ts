import { UIController, UIView } from '@tuval/forms';
export declare class OverviewController extends UIController {
    private selectedIndex;
    private showMenu;
    protected InitController(): void;
    LoadView(): UIView;
}
